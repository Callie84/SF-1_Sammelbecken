name: run
on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:
    inputs:
      reason:
        description: "Manueller Startgrund"
        required: false
        default: "manual"
  push:
    branches: [ main ]

concurrency:
  group: "run-${{ github.ref }}"
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

<<<<<<< Updated upstream
      - name: Git konfigurieren + Arbeitsbaum sÃƒÆ’Ã‚Â¤ubern
=======
      - name: Git konfigurieren + Arbeitsbaum sÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã¢â‚¬Å¡Ãƒâ€šÃ‚Â¤ubern
>>>>>>> Stashed changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git config core.autocrlf false
          git reset --hard

<<<<<<< Updated upstream
      - name: Pre-commit ausstehender ÃƒÆ’Ã¢â‚¬Å¾nderungen
=======
      - name: Pre-commit ausstehender ÃƒÆ’Ã†â€™Ãƒâ€ Ã¢â‚¬â„¢ÃƒÆ’Ã‚Â¢ÃƒÂ¢Ã¢â‚¬Å¡Ã‚Â¬Ãƒâ€¦Ã‚Â¾nderungen
>>>>>>> Stashed changes
        run: |
          git add -A
          git commit -m "ci: pre-commit pending changes" || true

      - name: Heartbeat-Datei
        run: |
          echo "$(date -u +%FT%TZ) $GITHUB_RUN_ID" >> ci_heartbeat.txt
      - name: Auto-commit (ohne .github)
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          skip_checkout: true
          branch: main
          commit_message: 'ci: auto-commit'
          file_pattern: |
            .
            :!**/.github/**